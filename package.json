{
  "name": "lupira-tasks-workspace",
  "private": true,
  "workspaces": [
    "packages/*",
    "src/LupiraTasksWeb.Client",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w src/LupiraTasksWeb.Client",
    "build": "npm run build -w src/LupiraTasksWeb.Client",
    "preview": "npm run preview -w src/LupiraTasksWeb.Client",
    "gen:api": "npm run gen:api -w src/LupiraTasksWeb.Client",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "overrides": {
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "eslint": "^10.4.1",
    "eslint-import-resolver-typescript": "^4.4.5",
    "eslint-plugin-boundaries": "^7.2.0",
    "typescript": "6.0.3",
    "typescript-eslint": "^8.65.0",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=24.19.0",
    "npm": ">=11.10.0"
  },
  "allowScripts": {
    "@sentry/cli": false,
    "esbuild": false,
    "fsevents": false,
    "unrs-resolver": false
  }
}
