{
  "name": "@lupira/tasks-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "routes": "node routes.mjs",
    "gen": "npm run spec && npm run routes",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "spec": "dotnet build ../../src/LupiraTasksBff/LupiraTasksBff.csproj -v q --nologo"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
